// Assuming you have an element representing the order status
const orderStatusElement = document.querySelector('.order-status');

// Get the updated order status from the server
const updatedStatus = "in-the-kitchen"; // Example status, you should get this from the server

// Update the class to apply the appropriate styles
orderStatusElement.className = order-{updatedStatus};